import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    
    
}

const userSlice = createSlice({
    name: 'userSlice',
    initialState,
    reducers: {

    }
})

// export karvana actions
export const { } =  userSlice.actions

// reducer default mate
export default userSlice.reducer